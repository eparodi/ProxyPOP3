#ifndef TPE_PROTOS_EXTERNAL_TRANSFORMATION_H
#define TPE_PROTOS_EXTERNAL_TRANSFORMATION_H

char * transform (char * mail, char * replacement_msg,
                  char * filtered_media_types,
                  char * version,
                  char * username,
                  char * server);

#endif //TPE_PROTOS_EXTERNAL_TRANSFORMATION_H
